/**
 * Régimen fiscal del tercero, con las opciones que maneja el formulario de
 * creación de cliente (distinto de JarvisTaxRegime, que se usa en las
 * credenciales de la empresa con otras opciones).
 */
export enum JarvisFiscalRegime {
  ORDINARY = 'ordinary',
  SIMPLE = 'simple',
  SPECIAL = 'special',
}
